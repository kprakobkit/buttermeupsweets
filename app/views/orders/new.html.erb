<div class="mih-1-1">
  <div class="container">
    <h1>Order Form</h1>
    <% if @order.errors.any? %>
      <div id="error_explanation">
      <p><%= pluralize(@order.errors.count, "error") %> prohibited
      this order from being saved:</p>
      <ul>
        <% @order.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <%= render "form" %>
    <p><em>Free delivery included with the price. Payment on delivery - we accept cash or check.</em></p>
  </div>
</div>
<div style="display: none" class="addItemsForm">
  <%= render "select_items" %>
</div>

